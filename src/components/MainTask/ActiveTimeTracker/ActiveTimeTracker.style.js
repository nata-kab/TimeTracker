import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  mainTitle: {
    marginTop: 45,
    alignItems: "flex-start",
    marginLeft: 40,
    marginRight: 40,
  },
});
